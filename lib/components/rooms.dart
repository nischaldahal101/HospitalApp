class Rooms {
  late String id, name, details, price, roomImage;
  late int availablenumber, version;

  Rooms(this.id, this.name, this.availablenumber, this.details, this.price,
      this.roomImage, this.version);
}
