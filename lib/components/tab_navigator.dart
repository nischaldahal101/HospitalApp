import 'package:flutter/material.dart';

class tab_navigator extends StatelessWidget {
  const tab_navigator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
